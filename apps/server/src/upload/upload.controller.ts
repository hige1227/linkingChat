// apps/server/src/upload/upload.controller.ts
import { Controller } from '@nestjs/common';

@Controller('upload')
export class UploadController {
  // Upload endpoints can be added here if needed
  // For now, upload is handled through ProfileController
}
