// CJS stub for @mariozechner/pi-ai — the real package is ESM-only and cannot be
// loaded by ts-jest in CJS mode.  Tests that need pi-ai behaviour mock it via
// jest.mock() or inject mock dependencies directly.
module.exports = {
  complete: jest.fn(),
};
