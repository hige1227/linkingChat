# 实测报告 2026-04-24

> 测试人: pmp@test.com + Codex 辅助核验
> 环境: Windows, Node.js, pnpm, Docker Desktop, PostgreSQL 16, Redis 7
> 目标: 验证最新 AI Gateway / Desktop server provider / packaged 默认 server 代理链路

---

## A. 测试背景

最近 3 次提交包含：

| 提交 | 内容 |
|------|------|
| `0004a5d` | Electron 升级到 `^35.7.5` |
| `2360873` | 新增 Server AI Gateway、`/ai/llm-token`、`/ai/llm-proxy`、Desktop server provider |
| `11dd9a3` | graphify 忽略 dist/node_modules 并重新生成图谱 |

本次实测重点不是 graphify，而是 `2360873` 引入的 AI Gateway 真实可用性。

---

## B. 预备修复与验证

### B1. 代码/配置修复

| 文件 | 修复 |
|------|------|
| `.gitignore` | 忽略 `token*.txt` 和误生成的 `apps/desktop/apps/` |
| `apps/desktop/src/main/agents/agent-provider.factory.ts` | packaged 模式默认 provider 改为 `server` |
| `apps/server/test/app.e2e-spec.ts` | `supertest` 改为默认导入，修复 type-check |
| `package.json` | 修复 Prisma 脚本，新增 `db:generate`，`db:migrate` 改为 `migrate deploy` |
| `apps/server/src/ai/ai-gateway.service.ts` | LLM provider fetch 增加重试、详细错误、DeepSeek 不可达时 fallback 到 Kimi |

### B2. 自动检查

| 命令 | 结果 | 备注 |
|------|------|------|
| `pnpm --filter @linkingchat/server type-check` | 通过 | 修复 Prisma Client 和 supertest 类型问题后通过 |
| `pnpm --filter @linkingchat/desktop type-check` | 通过 | Desktop 类型检查通过 |
| `pnpm --filter @linkingchat/server build` | 通过 | Server 构建通过 |
| `pnpm --filter @linkingchat/desktop build` | 通过 | Desktop 构建通过 |
| `pnpm db:generate` | 通过 | Prisma Client 生成成功 |
| `pnpm db:migrate` | 通过 | 当前无 pending migration |

### B3. 数据库迁移确认

| 项目 | 结果 |
|------|------|
| Postgres | healthy |
| Redis | healthy |
| `ai_usage` 表 | 已存在 |

确认命令：

```powershell
docker compose ps postgres redis
docker exec linkingchat-postgres psql -U linkingchat -d linkingchat -c "\dt ai_usage"
```

---

## C. 账号与登录链路

### C1. 测试账号

| 字段 | 值 |
|------|----|
| email | `pmp@test.com` |
| username | `pmp` |
| displayName | `pmp` |
| userId | `cmociagpr0002gdkoiboe7gnd` |

数据库确认：

```json
{
  "id": "cmociagpr0002gdkoiboe7gnd",
  "email": "pmp@test.com",
  "username": "pmp",
  "displayName": "pmp",
  "status": "ONLINE",
  "isEmailVerified": false
}
```

### C2. 登录状态

| 项目 | 结果 |
|------|------|
| 注册 | 通过 |
| 登录 | 通过 |
| refresh token | 产生 2 条 |
| Desktop 设备在线 | 通过 |
| LLM token 缓存 | 通过 |

LLM token 缓存：

| 项目 | 值 |
|------|----|
| token 是否存在 | 是 |
| token 长度 | 498 |
| 过期时间 | `2026-04-25 14:24:17` |

设备记录：

```json
{
  "id": "device-yehui-win32",
  "name": "yehui",
  "platform": "win32",
  "status": "ONLINE",
  "userId": "cmociagpr0002gdkoiboe7gnd"
}
```

---

## D. Desktop Dev 模式 server provider 测试

### D1. Provider 切换

在 Desktop DevTools 执行：

```js
await window.electronAPI.setAgentType('server')
await window.electronAPI.getAgentType()
```

结果：

```text
server
```

### D2. Bot 聊天链路

测试消息：

```text
请只回复：AI Gateway OK
```

UI 返回：

```text
AI Gateway OK
```

数据库证据：

```json
{
  "model": "deepseek-chat",
  "promptTokens": 11,
  "completionTokens": 3,
  "createdAt": "2026-04-24T06:18:47.683"
}
```

结论：

```text
Desktop UI -> Server /api/v1/ai/llm-proxy -> DeepSeek -> Server SSE -> Desktop UI
```

链路通过。

---

## E. 稳定性与回归测试

### E1. 连续聊天

用户连续发送多条：

```text
用一句话回答：1+2 等于几？
```

结果：

| 项目 | 结果 |
|------|------|
| UI 连续回复 | 通过 |
| 消息保存 | 通过 |
| `ai_usage` 递增 | 通过 |

阶段统计：

| 指标 | 数值 |
|------|------|
| `ai_usage` 总数 | 30 |
| prompt tokens | 431 |
| completion tokens | 255 |
| first usage | `2026-04-24T06:13:32.354` |
| last usage | `2026-04-24T06:25:35.148` |

### E2. 退出重进

| 项目 | 结果 |
|------|------|
| 关闭 Desktop 后重新启动 | 通过 |
| 不重新登录直接聊天 | 通过 |
| LLM token 缓存继续可用 | 通过 |

### E3. 重新登录

| 项目 | 结果 |
|------|------|
| 退出登录 | 通过 |
| 再登录 `pmp@test.com` | 通过 |
| LLM token 重新获取 | 通过 |
| token 过期时间刷新 | 通过 |

### E4. 限流

快速发送多条消息后检查 Redis：

| Key | 状态 |
|-----|------|
| `llm:rate:day:cmociagpr0002gdkoiboe7gnd` | 有计数 |
| `llm:rate:min:cmociagpr0002gdkoiboe7gnd` | 60 秒后过期，符合预期 |

观察：

- Redis 日计数曾达到 `35`
- `ai_usage` 曾为 `30`
- 差异原因：限流计数会记录请求进入，失败/中断/限流请求不一定写入 `ai_usage`

### E5. Provider 切换

| 操作 | 结果 |
|------|------|
| `setAgentType('openclaw')` | 可切换 |
| `setAgentType('server')` | 可切回 |
| 最终 provider | `server` |

---

## F. Packaged 版验证

### F1. 打目录包

命令：

```powershell
pnpm --filter @linkingchat/desktop dist:dir
```

结果：

| 项目 | 结果 |
|------|------|
| 构建 | 通过 |
| 输出路径 | `apps/desktop/dist/win-unpacked/LinkingChat.exe` |

构建提示：

| 提示 | 是否阻塞 |
|------|----------|
| `description is missed in package.json` | 否 |
| `author is missed in package.json` | 否 |
| `resources/hermes-env` 不存在 | 否 |
| Vite dynamic import chunk warning | 否 |

### F2. 本地 API 启动 packaged

因为 packaged 默认连接生产 API，本地测试使用：

```powershell
$env:API_BASE_URL="http://localhost:3008"
D:\myproject\LinkChat_new\apps\desktop\dist\win-unpacked\LinkingChat.exe
```

### F3. 无历史配置默认 provider

测试方法：

1. 临时移走 `linkingchat-agent.json`
2. 启动 packaged app
3. 检查重新生成的 store
4. 恢复原始 store

自动生成结果：

```json
{
  "agentType": "server"
}
```

结论：

```text
packaged 版在无历史配置时默认 provider = server
```

### F4. Packaged UI 聊天

测试消息：

```text
请只回复：packaged server OK
```

第一次结果：

```text
LLM provider unreachable: fetch failed
```

判定：

- 不是 Desktop provider 问题
- 不是 `/ai/llm-token` 问题
- 失败点在 `Server -> DeepSeek`

---

## G. DeepSeek 网络问题与 Kimi fallback

### G1. DeepSeek 失败现象

Server 日志显示：

```text
LLM provider fetch failed (1/2): fetch failed (UND_ERR_CONNECT_TIMEOUT: Connect Timeout Error (attempted address: api.deepseek.com:443, timeout: 10000ms))
LLM provider fetch failed (2/2): fetch failed (UND_ERR_CONNECT_TIMEOUT: Connect Timeout Error (attempted address: api.deepseek.com:443, timeout: 10000ms))
```

同时当前 shell 下直接 Node fetch 可以访问：

```powershell
node -e "fetch('https://api.deepseek.com').then(r=>console.log(r.status))"
```

返回：

```text
401
```

说明：

- 401 表示网络可达但未带鉴权
- Server 子进程访问 DeepSeek 偶发/持续超时
- 推测与本机代理/TUN 对不同进程的网络规则有关

### G2. Kimi 路径验证

同一个 server 进程测试 Kimi：

```text
model = moonshot-v1-8k
```

结果：

| 项目 | 结果 |
|------|------|
| Server -> Kimi | 通过 |
| SSE 返回 | 通过 |
| `ai_usage` 落库 | 通过 |

### G3. 新增 fallback 后复测

改动：

- 默认 DeepSeek 请求失败时重试一次
- 仍失败则 fallback 到 Kimi
- 明确记录底层错误
- API key 未配置时返回明确错误

复测结果：

| 项目 | 结果 |
|------|------|
| DeepSeek 超时时错误信息 | 更具体 |
| fallback 到 Kimi | 通过 |
| `ai_usage` 最新模型 | `kimi-k2-0905-preview` |

最新落库示例：

```json
{
  "model": "kimi-k2-0905-preview",
  "promptTokens": 0,
  "completionTokens": 0,
  "createdAt": "2026-04-24T07:00:41.072"
}
```

备注：

- Kimi SSE 未返回 usage 时，当前记录 token 为 0
- 这是 provider usage 数据兼容问题，不影响回复链路

---

## H. 其他观察

### H1. LlmRouterService fallback

用户后续日志：

```text
[LlmRouterService] [LLM FAIL] provider=deepseek task=whisper duration=3010ms tokens=0 (prompt=0 completion=0) error="This operation was aborted"
[LlmRouterService] [LLM OK [FALLBACK]] provider=kimi task=whisper duration=5200ms tokens=527 (prompt=500 completion=27)
```

结论：

- `LlmRouterService` 旧 AI 路由也存在 DeepSeek -> Kimi fallback
- Whisper 任务 fallback 正常
- 这条日志不是 `/ai/llm-proxy` 的 `AiGatewayService` 日志，但现象一致：DeepSeek 不稳定，Kimi 可兜底

### H2. 本地进程状态

曾出现一次额外启动 server 导致：

```text
EADDRINUSE: address already in use :::3008
```

原因：

- 端口 `3008` 已有 server 监听
- 后续测试应避免重复启动多个 `pnpm dev:server`

---

## I. 总结

### I1. 已通过

| 测试项 | 结论 |
|--------|------|
| 注册/登录 | 通过 |
| refresh token | 通过 |
| Desktop 设备在线 | 通过 |
| `/ai/llm-token` | 通过 |
| Desktop dev 手动切 `server` | 通过 |
| Desktop -> Server -> DeepSeek -> Desktop | 通过，存在网络偶发问题 |
| `ai_usage` 落库 | 通过 |
| Redis 限流计数 | 通过 |
| provider openclaw/server 切换 | 通过 |
| packaged 构建 | 通过 |
| packaged 无历史配置默认 `server` | 通过 |
| DeepSeek 失败时错误可观测性 | 已增强 |
| DeepSeek 失败 fallback 到 Kimi | 通过 |

### I2. 遗留风险

| 风险 | 影响 | 建议 |
|------|------|------|
| Server 子进程访问 DeepSeek 偶发超时 | UI 可能看到 provider unreachable | 当前已用 Kimi fallback 缓解；后续检查代理/TUN 规则 |
| Kimi SSE usage 可能为 0 | `ai_usage` token 统计不完整 | 后续可按 provider 兼容 usage 解析 |
| packaged 默认连接生产 API | 本地验证需要设置 `API_BASE_URL` | 本地测试继续用 `$env:API_BASE_URL="http://localhost:3008"` |
| 重复启动 server 会占用端口 | `EADDRINUSE` | 测试前确认 3008 只监听一个 server |

### I3. 当前最终结论

```text
AI Gateway 主链路已可用。
Desktop dev 模式可手动切 server 代理。
Packaged 版全新配置默认 server 代理。
DeepSeek 网络不稳定时，Server 可 fallback 到 Kimi，避免 UI 直接失败。
```

---

## J. Production P0-4 remediation

### J1. Symptom

Packaged Desktop without local `API_BASE_URL` could log in and send a message through production, but the bot response was:

```text
Server proxy returned 404
```

Direct production checks showed:

| Endpoint | Result before remediation |
|----------|---------------------------|
| `GET /api/v1/ai/health` | 200 |
| `POST /api/v1/ai/llm-proxy` | 404 |

Conclusion: the production server did not yet have the new `AiGatewayController` routes deployed.

### J2. Production server update

Actions performed:

| Step | Result |
|------|--------|
| Uploaded latest server/package sources to `/opt/linkchat` | PASS |
| Backed up previous production code | PASS |
| Rebuilt Docker service `server` | PASS |
| Restarted production `server` | PASS |
| Verified Nest routes for `AiGatewayController` | PASS |

Verified routes in production logs:

```text
Mapped {/api/v1/ai/llm-token, POST}
Mapped {/api/v1/ai/llm-proxy, POST}
```

### J3. Production DB migration

`npx prisma migrate deploy --schema prisma/schema.prisma` hung inside the production container, so the migration SQL was applied directly to PostgreSQL and the matching `_prisma_migrations` row was inserted.

Applied migration:

```text
20260424000000_add_ai_usage
```

Verification:

| Check | Result |
|-------|--------|
| `to_regclass('public.ai_usage')` | `ai_usage` |
| `_prisma_migrations` row | `20260424000000_add_ai_usage` |

### J4. Production API recheck

After deployment and migration:

| Endpoint | Probe | Result |
|----------|-------|--------|
| `POST /api/v1/ai/llm-token` | invalid app token | 401 Unauthorized |
| `POST /api/v1/ai/llm-proxy` | invalid LLM token | 401 Unauthorized |

Conclusion: the previous 404 is fixed. P0-4 is ready for a real packaged Desktop retest with a valid login session.

### J5. Production packaged retest

Retest account:

```text
ice@test.com
```

Prompt:

```text
请只回复：production server Po-4 OK
```

UI result:

```text
production server Po-4 OK
```

Production DB evidence:

| email | model | promptTokens | completionTokens | createdAt |
|-------|-------|--------------|------------------|-----------|
| `ice@test.com` | `deepseek-chat` | 15 | 7 | `2026-04-24 09:11:21.092 UTC` |

Sidebar status observation:

| Indicator | Meaning | Result |
|-----------|---------|--------|
| First dot | Chat WebSocket | Briefly gray after login, then green |
| Second dot | AI Agent/OpenClaw status | Green |

Conclusion: P0-4 passed after production server deployment and DB migration.

---

## K. P1-1 long streaming reply

Test account:

```text
ice@test.com
```

Prompt:

```text
写一段 500 字的产品介绍，主题是 LinkingChat 的智能协作能力。请分 3 段输出，并在最后一行只写：P1-1 LONG STREAM OK
```

Observed UI result:

| Item | Result |
|------|--------|
| Long bot reply | PASS |
| Final marker | `P1-1 LONG STREAM OK` |
| User prompt display | FAIL before fix: prompt was persisted but did not remain visible in the chat thread |

Production DB evidence:

| Evidence | Result |
|----------|--------|
| `ai_usage` | `ice@test.com`, `deepseek-chat`, `promptTokens=45`, `completionTokens=650`, `createdAt=2026-04-24 09:26:27.918 UTC` |
| `messages` user prompt | Present at `2026-04-24 09:26:16.335 UTC` |
| `messages` bot reply | Present at `2026-04-24 09:26:28.147 UTC` |

Root cause:

```text
Bot conversation send path persisted the user message by REST but did not add the REST response to the local chat store.
It relied on the chat WebSocket echo. If the socket was not connected or had not joined the room in time, the user message looked swallowed even though it was saved.
```

Fix:

| File | Change |
|------|--------|
| `apps/desktop/src/renderer/components/chat/MessageInput.tsx` | In bot conversations, parse the successful REST `/messages` response and call `addMessage` + `updateLastMessage`, matching the normal chat path. Existing store de-duplication handles later WebSocket echoes. |

Verification:

| Command | Result |
|---------|--------|
| `pnpm --filter @linkingchat/desktop type-check` | PASS |
| `pnpm --filter @linkingchat/server type-check` | PASS |
| `pnpm --filter @linkingchat/desktop dist:dir` | PASS |

## S. P2-4 Redis unavailable

Initial observation:

```text
With production Redis stopped, the first `/api/v1/ai/llm-proxy` request hung until the client timeout. Server did not crash, but the failure was not clear enough for users.
```

Fix applied:

| File | Change |
|------|--------|
| `apps/server/src/redis/redis.module.ts` | Disabled ioredis offline queue and capped retries so Redis dependency failures surface quickly. |
| `apps/server/src/ai/ai-gateway.service.ts` | Converted rate-limit Redis failures into a clear `Rate limit dependency unavailable` error before provider calls. |

Verification:

| Check | Result |
|-------|--------|
| Fault injection | Stopped production `redis` |
| Request | POST `/api/v1/ai/llm-proxy` with a temporary LLM token |
| Response time | About `435ms` |
| SSE error | `Rate limit dependency unavailable` |
| Server health during failure | `healthy` |
| `ai_usage` before/after | `71 -> 71` |
| Log evidence | `Rate limit dependency unavailable: Stream isn't writeable and enableOfflineQueue options is false` |
| Recovery | Restarted Redis and verified `redis=healthy`, `server=healthy` |
| Recovery smoke | After Redis recovery, a normal `/api/v1/ai/llm-proxy` request streamed `P2-4 RECOVERY SMOKE OK` and created 1 `deepseek-chat` usage row. |

Conclusion:

```text
P2-4 is PASS. If Redis is unavailable, AI Gateway now fails fast with a clear dependency error, does not call the provider, does not create usage rows, and production recovers after Redis is restored.
```

### L4. Stale packaged build finding

Observed after the duplicate-send fix:

```text
The packaged app still behaved like the old implementation.
```

Root cause:

```text
`pnpm --filter @linkingchat/desktop dist:dir` only ran `electron-builder --win --dir`; it did not run `electron-vite build` first. Source changes in `src/renderer` were not guaranteed to be present in `apps/desktop/out/renderer`, so the packaged app could include stale renderer assets.
```

Evidence:

| Check | Result |
|-------|--------|
| `apps/desktop/out/renderer/...js` before rebuild | still contained `sendOpenClawMessage(...).catch(...)` |
| `apps/desktop/out/renderer/...js` after `pnpm --filter @linkingchat/desktop build` | contains `await sendOpenClawMessage(content, botId, token)` |

Fix:

| File | Change |
|------|--------|
| `apps/desktop/package.json` | `dist` and `dist:dir` now run `pnpm build` before `electron-builder` |

Verification:

| Command | Result |
|---------|--------|
| `pnpm --filter @linkingchat/desktop build` | PASS |
| `pnpm --filter @linkingchat/desktop dist:dir` | PASS, now includes build step |

---

## M. P1-3 server restart while Desktop is open

### M1. Production server restart

Action:

```text
Restarted production `server` container while packaged Desktop remained open.
```

Server-side result:

| Check | Result |
|-------|--------|
| `docker compose restart server` | PASS |
| Health check | `healthy` |
| `DeviceGateway` initialized | PASS |
| `ChatGateway` initialized | PASS |
| `AiGatewayController` routes mapped | PASS |
| Device reconnect | PASS, device heartbeat resumed |

Observed Desktop result before fix:

| Item | Result |
|------|--------|
| First sidebar dot | Gray |
| Second sidebar dot | Green |
| Message send after restart | FAIL: user reported unable to send |

Root cause hypothesis:

```text
After server restart, the renderer chat Socket.IO connection could remain disconnected without a forced reconnect path. A send attempt during the restart window could also leave MessageInput waiting on a fetch/stream without timeout, keeping the send lock active.
```

Fix:

| File | Change |
|------|--------|
| `apps/desktop/src/renderer/hooks/useChatSocket.ts` | Added forced reconnect scheduling on `disconnect` and `connect_error`; refresh token is applied before reconnect. |
| `apps/desktop/src/renderer/components/chat/MessageInput.tsx` | Added timeout for message REST POST and bot reply wait, releasing the send lock if the server is unavailable. |
| `apps/desktop/src/main/agents/server.adapter.ts` | Added server proxy stream timeout that emits a clear timeout error instead of waiting indefinitely. |

Verification:

| Command | Result |
|---------|--------|
| `pnpm --filter @linkingchat/desktop type-check` | PASS |
| `pnpm --filter @linkingchat/server type-check` | PASS |
| `pnpm --filter @linkingchat/desktop build` | PASS |
| `pnpm --filter @linkingchat/desktop dist:dir` | PASS |

Current status:

```text
P1-3 exposed a reconnect/send-lock defect. The packaged app has been rebuilt with reconnect and timeout fixes. Retest required before marking P1-3 PASS.
```

### M2. Retest after reconnect and timeout fixes

Retest prompt:

```text
请只回复：P1-3 SERVER RESTART RETEST OK
```

Production evidence:

| Evidence | Result |
|----------|--------|
| `ChatGateway` reconnect | `2026-04-24 14:20:38.840 UTC` |
| User message | `2026-04-24 14:24:11.677 UTC` |
| Bot reply | `P1-3 SERVER RESTART RETEST OK` at `2026-04-24 14:24:12.825 UTC` |
| `ai_usage` | `ice@test.com`, `deepseek-chat`, `promptTokens=19`, `completionTokens=11`, `createdAt=2026-04-24 14:24:12.625 UTC` |

Conclusion:

```text
P1-3 is PASS after reconnect and timeout fixes.
```

---

## N. P1-4 fallback attempt

### N1. Normal-path prompt

Prompt:

```text
请只回复：P1-4 FALLBACK OK
```

Result:

| Evidence | Result |
|----------|--------|
| UI reply | `P1-4 FALLBACK OK` |
| User message | Present at `2026-04-24 14:29:40.709 UTC` |
| Bot reply | Present at `2026-04-24 14:29:42.011 UTC` |
| `ai_usage` | `deepseek-chat`, `promptTokens=16`, `completionTokens=8`, `createdAt=2026-04-24 14:29:41.826 UTC` |
| Server logs | `[LLM OK] provider=deepseek` |

Conclusion:

```text
This was not a fallback. DeepSeek succeeded normally.
```

### N2. Controlled DeepSeek failure attempt

Action:

```text
Temporarily changed production `DEEPSEEK_BASE_URL` to `http://127.0.0.1:9` and recreated the `server` container.
```

Server result:

| Check | Result |
|-------|--------|
| Server health | healthy |
| `DeviceGateway` initialized | PASS |
| `ChatGateway` initialized | PASS |
| `AiGatewayController` routes mapped | PASS |

Observed Desktop result:

| Item | Result |
|------|--------|
| Send button | Clickable |
| Message reaches production | FAIL |
| Bot reply | Not reached |

Recovery:

```text
Restored `DEEPSEEK_BASE_URL=https://api.deepseek.com` and recreated the production `server` container.
```

Follow-up Desktop fixes:

| File | Change |
|------|--------|
| `apps/desktop/src/renderer/hooks/useChatSocket.ts` | Reconnect now refreshes socket auth from the latest main-process token before each forced reconnect; connect errors refresh token every time instead of stopping after three attempts. |
| `apps/desktop/src/renderer/components/chat/MessageInput.tsx` | Failed sends restore the typed content instead of silently clearing it. |

Verification:

| Command | Result |
|---------|--------|
| `pnpm --filter @linkingchat/desktop type-check` | PASS |
| `pnpm --filter @linkingchat/server type-check` | PASS |
| `pnpm --filter @linkingchat/desktop build` | PASS |
| `pnpm --filter @linkingchat/desktop dist:dir` | PASS |

Current status:

```text
P1-4 is BLOCKED pending a fresh fallback retest. Production DeepSeek configuration has been restored.
```

### N3. Successful fallback retest

Preparation:

```text
Confirmed normal recovery message `RECOVERY AFTER P1-4 OK` persisted, then repeated controlled fault injection by setting `DEEPSEEK_BASE_URL=http://127.0.0.1:9` and recreating production `server`.
```

Retest prompt:

```text
请只回复：P1-4 FALLBACK REAL OK
```

Production evidence:

| Evidence | Result |
|----------|--------|
| User message | `2026-04-24 15:19:39.174 UTC` |
| Bot reply | `P1-4 FALLBACK REAL OK` at `2026-04-24 15:19:41.563 UTC` |
| `ai_usage` | `ice@test.com`, `moonshot-v1-8k`, `promptTokens=0`, `completionTokens=0`, `createdAt=2026-04-24 15:19:41.102 UTC` |
| DeepSeek failure log | `LLM provider fetch failed (1/2): fetch failed (bad port)` and `(2/2)` |
| Fallback log | `Falling back from deepseek-chat to moonshot-v1-8k: fetch failed (bad port)` |

Additional log evidence:

```text
[LLM FAIL] provider=deepseek task=whisper ... error="fetch failed"
[LLM OK [FALLBACK]] provider=kimi task=whisper ...
```

Recovery:

```text
Restored `DEEPSEEK_BASE_URL=https://api.deepseek.com`, recreated production `server`, and verified health returned to `healthy`.
```

Conclusion:

```text
P1-4 is PASS. DeepSeek failure falls back to Kimi/Moonshot and the packaged Desktop still receives a reply.
```

---

## O. P1-5 rate limit behavior

Test method:

```text
Used a temporary 10-minute LLM token for `ice@test.com` and sent 22 production `/api/v1/ai/llm-proxy` requests with `max_tokens=1`.
```

User:

| Field | Value |
|-------|-------|
| email | `ice@test.com` |
| userId | `cmnsytign000fo301cb0e4p2m` |

Result:

| Request range | Result |
|---------------|--------|
| 1-20 | OK |
| 21-22 | `Rate limit exceeded` |

Redis evidence:

| Key | Result |
|-----|--------|
| `llm:rate:min:cmnsytign000fo301cb0e4p2m` | reached `22`, then expired after about 60 seconds (`ttl=-2`) |
| `llm:rate:day:cmnsytign000fo301cb0e4p2m` | increased to `72`, TTL remained active |

Database evidence:

| Check | Result |
|-------|--------|
| `ai_usage` rows during the test window | 20 |
| model | `deepseek-chat` |
| rate-limited requests wrote usage | No |

Conclusion:

```text
P1-5 is PASS. Minute rate limiting triggers after 20 requests, returns a clear rate-limit error, and the minute key expires as expected.
```

---

## P. P2-1 logout clears LLM token

Pre-logout local store:

| Store | Result |
|-------|--------|
| `linkingchat-ai-gateway.json` | Had `llmToken`, length 498, expiry `2026-04-25 23:15:58` local time |

Action:

```text
User clicked Desktop logout.
```

Post-logout local store:

| Store | Result |
|-------|--------|
| `linkingchat-ai-gateway.json` | `{}` |
| `llmToken` | absent |
| `llmTokenExpiry` | absent |
| `linkingchat-auth.json` | `{}` |

Conclusion:

```text
P2-1 token clearing is PASS: both the app auth token store and the LLM proxy token store were cleared. The UI part is marked PARTIAL because the first sidebar dot may already have been gray before logout, so "logged-out chat blocked from a healthy connected chat state" still needs a focused retest.
```

Focused UI retest:

| Check | Result |
|-------|--------|
| Pre-logout state | User confirmed both sidebar dots were green |
| Account | `ice@test.com` |
| Action | User logged out |
| `linkingchat-ai-gateway.json` after logout | `{}` |
| `linkingchat-auth.json` after logout | `{}` |

Updated conclusion:

```text
P2-1 is PASS after focused retest from a healthy connected Desktop state.
```

---

## Q. P2-2 invalid LLM token rejected

Test:

```text
POST https://linkchat-api.matrix-ai.com.cn/api/v1/ai/llm-proxy
Authorization: Bearer invalid-token
```

Result:

| Check | Result |
|-------|--------|
| HTTP status | 401 |
| Error message | `Invalid or expired LLM token` |
| `ai_usage` before/after window | 0 rows |

Conclusion:

```text
P2-2 is PASS. Invalid LLM tokens are rejected before provider calls and do not create usage rows.
```

---

## R. P2-3 missing provider API key

Action:

```text
Temporarily recreated production `server` with `DEEPSEEK_API_KEY=` and `KIMI_API_KEY=` overriding env-file secrets.
```

Test:

```text
POST /api/v1/ai/llm-proxy with a temporary LLM token for `ice@test.com`.
```

Result:

| Check | Result |
|-------|--------|
| Error clarity | `LLM provider API key is not configured for deepseek-chat` |
| Ambiguous fetch error | No |
| `ai_usage` rows in surrounding 5-minute window | 0 |

Recovery:

```text
Removed the temporary API key overrides, recreated production `server`, and verified health returned to `healthy`.
```

Conclusion:

```text
P2-3 is PASS. Missing provider credentials return a clear configuration error and do not create usage rows.
```

Current status:

```text
P1-1 long reply and persistence passed. The user-message display bug has been fixed and the packaged directory has been rebuilt. A UI retest is needed to confirm the sent prompt stays visible.
```

### K1. User-message display retest

Prompt:

```text
请只回复：P1-1 USER MESSAGE VISIBLE OK
```

Result:

| Item | Result |
|------|--------|
| User message remains visible | PASS |
| Bot reply | `P1-1 USER MESSAGE VISIBLE OK` |
| Production DB | Two real sends and two real bot replies were recorded, matching the UI screenshot |

Conclusion:

```text
P1-1 is PASS. The earlier swallowed-message symptom was fixed by immediately adding the REST-saved user message to the local chat store in bot conversations.
```

---

## L. P1-2 interrupt and recovery

### L1. Long reply while switching conversations

Prompt:

```text
请写一篇 1200 字的长文，主题是 LinkingChat 如何帮助团队进行智能协作、自动总结、任务跟进和知识沉淀。请慢慢分段输出，最后一行只写：P1-2 INTERRUPT LONG OK
```

User action:

```text
Sent the long prompt, switched from Supervisor to Coding Bot during generation, then observed the final reply.
```

Result:

| Item | Result |
|------|--------|
| UI freeze | No freeze observed |
| Long reply completion | PASS |
| Final marker | `P1-2 INTERRUPT LONG OK` |
| Production user message | Present at `2026-04-24 09:38:55.682 UTC` |
| Production bot reply | Present at `2026-04-24 09:39:07.887 UTC` |
| `ai_usage` | `deepseek-chat`, `promptTokens=55`, `completionTokens=716`, `createdAt=2026-04-24 09:39:07.662 UTC` |

### L2. Recovery short prompt duplicate-send issue

Recovery prompt:

```text
请只回复：P1-2 AFTER INTERRUPT OK
```

Observed:

```text
The first send appeared to hang for a few seconds. Retrying caused two user messages and two bot replies to appear.
```

Production DB confirmed two real sends, not duplicate rendering:

| Content | CreatedAt |
|---------|-----------|
| user prompt | `2026-04-24 09:41:08.669 UTC` |
| user prompt | `2026-04-24 09:41:08.703 UTC` |
| bot reply | `2026-04-24 09:41:09.913 UTC` |
| bot reply | `2026-04-24 09:41:10.242 UTC` |

Root cause:

```text
The bot conversation send path had no synchronous duplicate-submit guard. React `sending` state is asynchronous, so two rapid Enter/click events could dispatch before the disabled state rendered.
```

Fix:

| File | Change |
|------|--------|
| `apps/desktop/src/renderer/components/chat/MessageInput.tsx` | Added `sendingRef` as a synchronous guard for all sends. Bot conversation sends now set `sendingRef` and `sending` before clearing the input, then release the guard after the REST save and stream dispatch are started. |

Verification:

| Command | Result |
|---------|--------|
| `pnpm --filter @linkingchat/desktop type-check` | PASS |
| `pnpm --filter @linkingchat/server type-check` | PASS |
| `pnpm --filter @linkingchat/desktop dist:dir` | PASS |

Current status:

```text
P1-2 interruption behavior passed, but recovery exposed duplicate-send behavior. The duplicate-send bug is fixed and packaged output has been rebuilt. Retest required before marking P1-2 PASS.
```

### L3. Duplicate-send stress retest

Retest prompt:

```text
请只回复：P1-2 AFTER INTERRUPT RETEST OK
```

Observed:

```text
The UI showed multiple user messages and multiple bot replies when the prompt was submitted repeatedly before the prior bot reply completed.
```

Production DB evidence:

| Item | Result |
|------|--------|
| User messages | 6 real persisted user messages |
| Bot replies | 6 real persisted bot replies |
| Time window | `2026-04-24 09:50:37.973 UTC` to `2026-04-24 09:50:42.982 UTC` |
| `ai_usage` | 6 real `deepseek-chat` usage rows with `promptTokens=19`, `completionTokens=11` |

Updated fix:

| File | Change |
|------|--------|
| `apps/desktop/src/renderer/components/chat/MessageInput.tsx` | Bot conversation `sendingRef` now remains locked until `sendOpenClawMessage` completes. The previous fix released the lock immediately after starting the stream, which still allowed repeated submits while the bot reply was in flight. |

Verification:

| Command | Result |
|---------|--------|
| `pnpm --filter @linkingchat/desktop type-check` | PASS |
| `pnpm --filter @linkingchat/server type-check` | PASS |
| `pnpm --filter @linkingchat/desktop dist:dir` | PASS |

---

## T. Current testing status

| Area | Status |
|------|--------|
| P0 packaged build/default/local/production smoke | PASS |
| P1 streaming/reconnect/fallback/rate-limit behavior | PASS |
| P2 logout/token/config/Redis failure behavior | PASS |
| Production health after fault tests | `server=healthy`, `redis=healthy` |
| Local verification after final fixes | `@linkingchat/server type-check` PASS, `@linkingchat/server build` PASS, `@linkingchat/desktop type-check` PASS, `@linkingchat/desktop build` PASS |
| Focused unit coverage | `pnpm --filter @linkingchat/server test -- ai-gateway.service.spec.ts` PASS, 3 tests |
| Packaged output | First `dist:dir` attempt was blocked by running `dist\win-unpacked` Desktop processes; after stopping only those workspace packaged processes, `pnpm --filter @linkingchat/desktop dist:dir` PASS |
| Packaged launch smoke | `apps\desktop\dist\win-unpacked\LinkingChat.exe` launched and created `LinkingChat.exe` processes |

Conclusion:

```text
The planned AI Gateway packaged Desktop test set is complete. Release hygiene checks are complete: diff reviewed, focused regression tests added, production health confirmed, and unpacked packaged Desktop output rebuilt successfully.
```
